import { registerRootComponent } from 'expo';
import App from './App';

// Ponto de entrada do Expo. Registra o componente raiz tanto para
// nativo quanto para web (Metro).
registerRootComponent(App);
